# build output
dist/
.astro/

# dependencies
node_modules/

# environment — never commit secrets
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# local scratch
/tmp/
*.local

# raw Zillow downloads — never committed (constitution V: no raw-dataset redistribution)
.cache/

# live map build output (Phase D) — the committed CI-diffed artifact is the
# fixture SVG; the live build writes here so it can't clobber it (finding #12)
/data/map/metro-map.live.svg
/data/latest.json

# playwright artifacts
test-results/
playwright-report/
